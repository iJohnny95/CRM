{
    "name": "lead-generator-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "cd crm && node server.mjs",
        "install-all": "npm install --prefix crm && pip install -r requirements.txt"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}